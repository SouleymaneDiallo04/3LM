; Le master php-fpm tourne en root (nécessaire pour écrire sur les fd
; stdout/stderr du conteneur) ; les workers, eux, s'exécutent en `app`
; (uid 1000, aligné sur l'hôte) — moindre privilège pour le code applicatif.
[www]
user = app
group = app
